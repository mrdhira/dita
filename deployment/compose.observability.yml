# Opt-in metrics stack, behind the `observability` profile so `docker compose up` does not
# grow by default. vmui is at http://localhost:8428/vmui, which is why there is no Grafana.

services:
  victoria-metrics:
    image: victoriametrics/victoria-metrics:v1.151.0
    container_name: victoria-metrics
    profiles: ["observability"]
    command:
      - "-promscrape.config=/etc/vm/scrape.yml"
      - "-storageDataPath=/victoria-metrics-data"
      - "-retentionPeriod=15d"
      - "-httpListenAddr=:8428"
    ports:
      # vmui and the query API. Bound to loopback: this is a dev tool, not a service.
      - "127.0.0.1:8428:8428"
    volumes:
      - ./observability/scrape.yml:/etc/vm/scrape.yml:ro
      - victoria-metrics-data:/victoria-metrics-data
    mem_limit: 1g
    restart: unless-stopped

volumes:
  victoria-metrics-data:
