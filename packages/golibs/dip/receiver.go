package dip

// The receiver role is not implemented in Go. DIP expects a package to support both
// directions, but in this repository the receiver is Python -- packages/pylibs/dip carries
// that half -- so a Go receiver here would be code with no caller. It is written when
// something needs it.
